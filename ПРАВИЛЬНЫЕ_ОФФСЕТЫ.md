# ✅ ПРАВИЛЬНЫЕ ОФФСЕТЫ (1.32 GB версия)

## 📊 СРАВНЕНИЕ ВЕРСИЙ

| Компонент                        | 1.18 GB (СТАРАЯ)      | 1.32 GB (ТЕКУЩАЯ)         | Статус        |
| -------------------------------- | --------------------- | ------------------------- | ------------- |
| **PlayerManager → playersList**  | 0x18 ❌               | **0x28** ✅               | ИСПРАВЛЕНО    |
| **playersList → count**          | 0x20 ✅               | **0x20** ✅               | БЕЗ ИЗМЕНЕНИЙ |
| **playersList → firstPlayerPtr** | 0x30 ❌               | **0x18** ✅               | ИСПРАВЛЕНО    |
| **firstPlayerPtr → player[i]**   | 0x00 + (i \* 0x18) ❌ | **0x30 + (i \* 0x18)** ✅ | ИСПРАВЛЕНО    |
| **player → position chain**      | 0x98 → 0xB8 → 0x14 ✅ | **0x98 → 0xB8 → 0x14** ✅ | БЕЗ ИЗМЕНЕНИЙ |

---

## 🎯 ТЕКУЩАЯ РЕАЛИЗАЦИЯ (ПРАВИЛЬНАЯ)

### Файл: `External 2/bycmd/src/func/visuals.cpp` (строки 635-655)

```cpp
// === JNI WORKING OFFSETS (1.32 GB version) ===
// PlayerManager + 0x28 → entityList (playersList)
uint64_t playersList = rpm<uint64_t>(PlayerManager + 0x28);
if (!playersList || playersList < 0x10000 || playersList > 0x7FFFFFFFFFFF) return;

// playersList + 0x20 → count
int playerCount = rpm<int>(playersList + 0x20);
if (playerCount < 0 || playerCount > 100) return;

// playersList + 0x18 → firstPlayerPtr
uint64_t firstPlayerPtr = rpm<uint64_t>(playersList + 0x18);
if (!firstPlayerPtr || firstPlayerPtr < 0x10000 || firstPlayerPtr > 0x7FFFFFFFFFFF) return;

// === JNI: Iterate players ===
for (int i = 0; i < playerCount; i++)
{
    // firstPlayerPtr + 0x30 + (i * 0x18) → player
    uint64_t player = rpm<uint64_t>(firstPlayerPtr + 0x30 + (i * 0x18));
    if (!player || player < 0x10000 || player > 0x7FFFFFFFFFFF || player == LocalPlayer) continue;

    int playerTeam = rpm<int>(player + 0x79);
    if (playerTeam == LocalTeam) continue;

    // === JNI WORKING POSITION CHAIN: +0x98 → +0xB8 → +0x14 ===
    Vector3 position;
    {
        uint64_t ptr1 = rpm<uint64_t>(player + 0x98);
        if (!ptr1 || ptr1 < 0x10000) continue;

        uint64_t ptr2 = rpm<uint64_t>(ptr1 + 0xB8);
        if (!ptr2 || ptr2 < 0x10000) continue;

        position = rpm<Vector3>(ptr2 + 0x14);
    }
    if (position == Vector3(0, 0, 0)) continue;

    // ... остальной код ESP ...
}
```

---

## 📖 ИСТОЧНИК ОФФСЕТОВ

### Файл: `jni/jni/includes/uses.h` (строки 56-170)

```cpp
namespace offsets {
    inline int playerManager_addr = 151547120;

    inline int localPlayer_ = 0x70;
    inline int entityList = 0x28;              // ← PlayerManager → playersList
    inline int entityList_count = 0x20;        // ← playersList → count

    inline int pos_ptr1 = 0x98;                // ← player → ptr1
    inline int pos_ptr2 = 0xB8;                // ← ptr1 → ptr2
    inline int pos_ptr3 = 0x14;                // ← ptr2 → Vector3

    inline int player_ptr1 = 0x18;             // ← playersList → firstPlayerPtr
    inline int player_ptr2 = 0x30;             // ← firstPlayerPtr base offset
    inline int player_ptr3 = 0x18;             // ← stride (размер элемента)
}
```

### Файл: `jni/jni/main.cpp` (строки 110-120)

```cpp
for (int i = 0; i < functions.playerCount; i++) {
    uint64_t playerPtr1 = mem.read<uint64_t>(playersList + offsets::player_ptr1);  // 0x18
    if (!playerPtr1 || playerPtr1 < 0x10000 || playerPtr1 > 0x7fffffffffff) continue;

    uint64_t player = mem.read<uint64_t>(playerPtr1 + offsets::player_ptr2 + offsets::player_ptr3 * i);  // 0x30 + 0x18 * i
    if (!player || player < 0x10000 || player > 0x7fffffffffff) continue;

    // ... чтение позиции ...
    uint64_t pos_ptr1 = mem.read<uint64_t>(player + offsets::pos_ptr1);  // 0x98
    if (!pos_ptr1 || pos_ptr1 < 0x10000 || pos_ptr1 > 0x7fffffffffff) continue;
    uint64_t pos_ptr2 = mem.read<uint64_t>(pos_ptr1 + offsets::pos_ptr2);  // 0xB8
    if (!pos_ptr2 || pos_ptr2 < 0x10000 || pos_ptr2 > 0x7fffffffffff) continue;
    Vector3 position = mem.read<Vector3>(pos_ptr2 + offsets::pos_ptr3);  // 0x14
}
```

---

## 🔍 ДЕТАЛЬНОЕ ОБЪЯСНЕНИЕ

### 1. Чтение списка игроков

```
PlayerManager (0x7a12345678)
    ↓ +0x28 (entityList)
playersList (0x7a23456789)
    ↓ +0x20 (entityList_count)
playerCount = 10
    ↓ +0x18 (player_ptr1)
firstPlayerPtr (0x7a3456789a)
```

### 2. Итерация по игрокам

```
firstPlayerPtr (0x7a3456789a)
    ↓ +0x30 + (0 * 0x18) = +0x30
player[0] (0x7a456789ab)

firstPlayerPtr (0x7a3456789a)
    ↓ +0x30 + (1 * 0x18) = +0x48
player[1] (0x7a567890bc)

firstPlayerPtr (0x7a3456789a)
    ↓ +0x30 + (2 * 0x18) = +0x60
player[2] (0x7a678901cd)
```

### 3. Чтение позиции игрока

```
player (0x7a456789ab)
    ↓ +0x98 (pos_ptr1)
ptr1 (0x7a56789abc)
    ↓ +0xB8 (pos_ptr2)
ptr2 (0x7a6789abcd)
    ↓ +0x14 (pos_ptr3)
Vector3 position = { x: 123.45, y: 67.89, z: 234.56 }
```

---

## 🐛 ОТЛАДОЧНЫЙ КОД

Если ESP не работает, добавьте этот код после строки 635 в `visuals.cpp`:

```cpp
// DEBUG: Print player info
static int debug_frame = 0;
if (debug_frame++ % 60 == 0) { // Print every 60 frames (~1 second)
    FILE* f = fopen("/sdcard/esp_debug.txt", "a");
    if (f) {
        fprintf(f, "\n=== ESP DEBUG Frame %d ===\n", debug_frame);
        fprintf(f, "PlayerManager: 0x%llx\n", PlayerManager);
        fprintf(f, "playersList: 0x%llx\n", playersList);
        fprintf(f, "playerCount: %d\n", playerCount);
        fprintf(f, "firstPlayerPtr: 0x%llx\n", firstPlayerPtr);
        fprintf(f, "LocalPlayer: 0x%llx\n", LocalPlayer);
        fprintf(f, "LocalPosition: (%.2f, %.2f, %.2f)\n",
                LocalPosition.x, LocalPosition.y, LocalPosition.z);
        fprintf(f, "ViewMatrix.m11: %.4f\n", ViewMatrix.m11);

        // Print first 3 players
        for (int i = 0; i < std::min(playerCount, 3); i++) {
            uint64_t player = rpm<uint64_t>(firstPlayerPtr + 0x30 + (i * 0x18));
            fprintf(f, "  Player[%d]: 0x%llx\n", i, player);

            if (player && player > 0x10000) {
                uint64_t ptr1 = rpm<uint64_t>(player + 0x98);
                fprintf(f, "    pos_ptr1: 0x%llx\n", ptr1);

                if (ptr1 && ptr1 > 0x10000) {
                    uint64_t ptr2 = rpm<uint64_t>(ptr1 + 0xB8);
                    fprintf(f, "    pos_ptr2: 0x%llx\n", ptr2);

                    if (ptr2 && ptr2 > 0x10000) {
                        Vector3 pos = rpm<Vector3>(ptr2 + 0x14);
                        fprintf(f, "    position: (%.2f, %.2f, %.2f)\n", pos.x, pos.y, pos.z);
                    }
                }
            }
        }

        fclose(f);
    }
}
```

### Как проверить лог:

```bash
adb pull /sdcard/esp_debug.txt
cat esp_debug.txt
```

### Ожидаемый вывод:

```
=== ESP DEBUG Frame 60 ===
PlayerManager: 0x7a12345678
playersList: 0x7a23456789
playerCount: 10
firstPlayerPtr: 0x7a3456789a
LocalPlayer: 0x7a456789ab
LocalPosition: (123.45, 67.89, 234.56)
ViewMatrix.m11: 1.7321
  Player[0]: 0x7a567890bc
    pos_ptr1: 0x7a6789abcd
    pos_ptr2: 0x7a789abcde
    position: (145.67, 68.12, 256.78)
  Player[1]: 0x7a890bcdef
    pos_ptr1: 0x7a9abcdef0
    pos_ptr2: 0x7aabcdef01
    position: (167.89, 69.34, 278.90)
```

---

## ✅ ПРОВЕРКА ПРАВИЛЬНОСТИ

### Контрольные точки:

1. ✅ **PlayerManager не NULL** (> 0x10000)
2. ✅ **playersList не NULL** (> 0x10000)
3. ✅ **playerCount в диапазоне 0-100**
4. ✅ **firstPlayerPtr не NULL** (> 0x10000)
5. ✅ **player[i] не NULL** (> 0x10000)
6. ✅ **pos_ptr1 не NULL** (> 0x10000)
7. ✅ **pos_ptr2 не NULL** (> 0x10000)
8. ✅ **position не (0, 0, 0)**
9. ✅ **ViewMatrix.m11 не 0**

### Если какая-то проверка не проходит:

| Проблема             | Причина                                  | Решение               |
| -------------------- | ---------------------------------------- | --------------------- |
| PlayerManager = 0x0  | Игра не загрузилась                      | Подождите 5-10 секунд |
| playersList = 0x0    | Неправильный offset 0x28                 | Проверьте dump.cs     |
| firstPlayerPtr = 0x0 | Неправильный offset 0x18                 | Проверьте dump.cs     |
| player[i] = 0x0      | Неправильный offset 0x30 или stride 0x18 | Проверьте dump.cs     |
| position = (0, 0, 0) | Неправильная цепочка 0x98 → 0xB8 → 0x14  | Проверьте dump.cs     |

---

## 🚀 КОМПИЛЯЦИЯ И ТЕСТИРОВАНИЕ

```bash
cd "External 2/bycmd"
./build.bat
```

Затем:

1. Запустите Standoff 2
2. Зайдите в матч
3. Включите ESP в меню
4. Проверьте, что враги отображаются

---

## 📝 ИСТОРИЯ ИЗМЕНЕНИЙ

### v2 (ТЕКУЩАЯ) - 1.32 GB версия

- ✅ PlayerManager + **0x28** → playersList (было 0x18)
- ✅ playersList + **0x18** → firstPlayerPtr (было 0x30)
- ✅ firstPlayerPtr + **0x30** + (i \* 0x18) → player[i] (было 0x00)
- ✅ Позиция: 0x98 → 0xB8 → 0x14 (без изменений)

### v1 (СТАРАЯ) - 1.18 GB версия

- ❌ PlayerManager + 0x18 → playersList (НЕПРАВИЛЬНО)
- ❌ playersList + 0x30 → firstPlayerPtr (НЕПРАВИЛЬНО)
- ❌ firstPlayerPtr + 0x00 + (i \* 0x18) → player[i] (НЕПРАВИЛЬНО)
- ✅ Позиция: 0x98 → 0xB8 → 0x14 (правильно)

---

**Теперь оффсеты правильные для версии 1.32 GB!** 🎯
